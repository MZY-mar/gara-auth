server:
  port: 8091
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gera
    username: postgres
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
gera:
  email: http://localhost:9094
